# 快速开始指南

> 从零开始搭建项目的详细步骤

---

## 📋 前置准备

### 1. 环境要求

- **Node.js**：v16+ （推荐 v18 或 v20 LTS）
- **npm**：v8+ 或 **yarn**、**pnpm**
- **数据库**：MySQL 5.7+ 或 PostgreSQL 12+
- **代码编辑器**：VS Code（推荐）

### 2. 安装 Node.js

**Windows**：
- 访问 [nodejs.org](https://nodejs.org/)
- 下载并安装 LTS 版本
- 验证安装：
```bash
node -v
npm -v
```

**Mac**：
```bash
# 使用 Homebrew
brew install node
```

### 3. 安装数据库

**MySQL（Windows）**：
- 下载 [MySQL Installer](https://dev.mysql.com/downloads/installer/)
- 安装时设置 root 密码
- 启动 MySQL 服务

**或使用 Docker**：
```bash
docker run -d \
  --name mysql \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=123456 \
  mysql:8
```

---

## 🚀 第一阶段：项目初始化

### 步骤 1：创建项目目录结构

在项目根目录执行：

```bash
# 创建目录结构
mkdir -p src/{config,models,controllers,services,middlewares,routes,utils}
mkdir -p uploads logs

# 创建基础文件
touch src/app.js
touch server.js
touch .env
touch .gitignore
touch README.md
```

### 步骤 2：初始化 package.json

```bash
npm init -y
```

编辑 `package.json`，添加启动脚本：

```json
{
  "name": "node_lean",
  "version": "1.0.0",
  "description": "Node.js 后台管理系统学习项目",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": ["node", "express", "admin"],
  "author": "your-name",
  "license": "ISC"
}
```

### 步骤 3：安装依赖

```bash
# 核心依赖
npm install express sequelize mysql2 jsonwebtoken bcrypt joi multer winston morgan dotenv cors helmet

# 开发依赖
npm install --save-dev nodemon
```

**依赖说明**：
- `express`：Web 框架
- `sequelize`：ORM
- `mysql2`：MySQL 驱动
- `jsonwebtoken`：JWT 认证
- `bcrypt`：密码加密
- `joi`：参数校验
- `multer`：文件上传
- `winston`：日志管理
- `morgan`：HTTP 日志
- `dotenv`：环境变量
- `cors`：跨域处理
- `helmet`：安全增强
- `nodemon`：开发自动重启

### 步骤 4：配置环境变量

创建 `.env` 文件：

```env
# 服务配置
PORT=3000
NODE_ENV=development

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=node_lean
DB_USER=root
DB_PASSWORD=123456

# JWT 配置
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRES_IN=24h

# 文件上传配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
```

**重要**：`.env` 文件包含敏感信息，不要提交到 Git！

### 步骤 5：配置 .gitignore

创建 `.gitignore` 文件：

```gitignore
# 依赖
node_modules/

# 环境变量
.env
.env.local
.env.*.local

# 日志
logs/
*.log

# 上传文件
uploads/

# 系统文件
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 数据库
*.sqlite
*.db
```

---

## 💻 第一阶段：编写基础代码

### 1. 统一返回结构 (src/utils/response.js)

```javascript
/**
 * 统一成功响应
 */
const success = (data = null, message = 'success', code = 200) => {
  return {
    code,
    message,
    data
  };
};

/**
 * 统一错误响应
 */
const error = (message = 'error', code = 400, error = null) => {
  return {
    code,
    message,
    error
  };
};

/**
 * 统一分页响应
 */
const paginate = (items, page, pageSize, total) => {
  return success({
    items,
    pagination: {
      page: parseInt(page),
      pageSize: parseInt(pageSize),
      total,
      totalPages: Math.ceil(total / pageSize)
    }
  });
};

module.exports = {
  success,
  error,
  paginate
};
```

### 2. 日志工具 (src/utils/logger.js)

```javascript
const winston = require('winston');
const path = require('path');

// 定义日志格式
const logFormat = winston.format.combine(
  winston.format.timestamp({ format: 'YYYY-MM-DD HH:mm:ss' }),
  winston.format.errors({ stack: true }),
  winston.format.printf(({ timestamp, level, message, stack }) => {
    return `${timestamp} [${level.toUpperCase()}]: ${stack || message}`;
  })
);

// 创建 logger 实例
const logger = winston.createLogger({
  level: process.env.NODE_ENV === 'production' ? 'info' : 'debug',
  format: logFormat,
  transports: [
    // 错误日志
    new winston.transports.File({
      filename: path.join('logs', 'error.log'),
      level: 'error',
      maxsize: 5242880, // 5MB
      maxFiles: 5
    }),
    // 所有日志
    new winston.transports.File({
      filename: path.join('logs', 'combined.log'),
      maxsize: 5242880,
      maxFiles: 5
    })
  ]
});

// 开发环境同时输出到控制台
if (process.env.NODE_ENV !== 'production') {
  logger.add(new winston.transports.Console({
    format: winston.format.combine(
      winston.format.colorize(),
      winston.format.simple()
    )
  }));
}

module.exports = logger;
```

### 3. 统一异常处理中间件 (src/middlewares/errorHandler.js)

```javascript
const logger = require('../utils/logger');
const { error } = require('../utils/response');

/**
 * 统一异常处理中间件
 */
const errorHandler = (err, req, res, next) => {
  // 记录错误日志
  logger.error(`${req.method} ${req.path} - ${err.message}`, {
    error: err.stack,
    user: req.user?.id,
    ip: req.ip
  });

  // 已知的业务错误
  if (err.name === 'ValidationError') {
    return res.status(400).json(error(err.message, 400, err.details));
  }

  if (err.name === 'UnauthorizedError' || err.message === 'Unauthorized') {
    return res.status(401).json(error('未授权，请先登录', 401));
  }

  if (err.name === 'ForbiddenError' || err.message === 'Forbidden') {
    return res.status(403).json(error('无权限访问', 403));
  }

  if (err.name === 'NotFoundError') {
    return res.status(404).json(error('资源不存在', 404));
  }

  // 数据库错误
  if (err.name === 'SequelizeUniqueConstraintError') {
    return res.status(409).json(error('数据已存在', 409));
  }

  if (err.name === 'SequelizeValidationError') {
    return res.status(400).json(error('数据验证失败', 400, err.errors));
  }

  // 未知错误
  const statusCode = err.statusCode || 500;
  const message = process.env.NODE_ENV === 'production' 
    ? '服务器错误' 
    : err.message;

  res.status(statusCode).json(error(message, statusCode));
};

/**
 * 404 处理
 */
const notFound = (req, res) => {
  res.status(404).json(error('接口不存在', 404));
};

module.exports = {
  errorHandler,
  notFound
};
```

### 4. 数据库配置 (src/config/database.js)

```javascript
const { Sequelize } = require('sequelize');
const logger = require('../utils/logger');

// 创建 Sequelize 实例
const sequelize = new Sequelize(
  process.env.DB_NAME,
  process.env.DB_USER,
  process.env.DB_PASSWORD,
  {
    host: process.env.DB_HOST,
    port: process.env.DB_PORT,
    dialect: 'mysql',
    logging: (msg) => logger.debug(msg),
    pool: {
      max: 5,
      min: 0,
      acquire: 30000,
      idle: 10000
    },
    timezone: '+08:00'
  }
);

// 测试连接
const testConnection = async () => {
  try {
    await sequelize.authenticate();
    logger.info('数据库连接成功');
  } catch (error) {
    logger.error('数据库连接失败:', error);
    process.exit(1);
  }
};

module.exports = {
  sequelize,
  testConnection
};
```

### 5. 应用入口 (src/app.js)

```javascript
const express = require('express');
const cors = require('cors');
const helmet = require('helmet');
const morgan = require('morgan');
const path = require('path');
const logger = require('./utils/logger');
const { errorHandler, notFound } = require('./middlewares/errorHandler');
const { success } = require('./utils/response');

// 创建 Express 应用
const app = express();

// 安全增强
app.use(helmet());

// 跨域处理
app.use(cors());

// 请求体解析
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// HTTP 请求日志
if (process.env.NODE_ENV !== 'production') {
  app.use(morgan('dev'));
}

// 静态文件服务（可选）
app.use('/uploads', express.static(path.join(__dirname, '../uploads')));

// 健康检查
app.get('/health', (req, res) => {
  res.json(success({ status: 'ok', timestamp: new Date() }));
});

// API 路由（后续添加）
// app.use('/api/auth', require('./routes/auth'));
// app.use('/api/users', require('./routes/user'));
// app.use('/api/tickets', require('./routes/ticket'));
// app.use('/api/files', require('./routes/file'));

// 404 处理
app.use(notFound);

// 统一异常处理
app.use(errorHandler);

module.exports = app;
```

### 6. 服务启动文件 (server.js)

```javascript
require('dotenv').config();
const app = require('./src/app');
const { testConnection, sequelize } = require('./src/config/database');
const logger = require('./src/utils/logger');

const PORT = process.env.PORT || 3000;

// 启动服务
const startServer = async () => {
  try {
    // 测试数据库连接
    await testConnection();

    // 同步数据库（开发环境，生产环境使用迁移）
    if (process.env.NODE_ENV === 'development') {
      await sequelize.sync({ alter: false });
      logger.info('数据库同步完成');
    }

    // 启动 HTTP 服务
    app.listen(PORT, () => {
      logger.info(`服务已启动：http://localhost:${PORT}`);
      logger.info(`环境：${process.env.NODE_ENV}`);
    });
  } catch (error) {
    logger.error('服务启动失败:', error);
    process.exit(1);
  }
};

// 优雅关闭
process.on('SIGTERM', () => {
  logger.info('收到 SIGTERM 信号，关闭服务...');
  sequelize.close();
  process.exit(0);
});

startServer();
```

---

## ✅ 测试基础功能

### 1. 创建数据库

登录 MySQL：
```bash
mysql -u root -p
```

创建数据库：
```sql
CREATE DATABASE node_lean CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 2. 启动项目

```bash
npm run dev
```

你应该看到：
```
[INFO]: 数据库连接成功
[INFO]: 数据库同步完成
[INFO]: 服务已启动：http://localhost:3000
[INFO]: 环境：development
```

### 3. 测试健康检查接口

浏览器访问：`http://localhost:3000/health`

或使用 curl：
```bash
curl http://localhost:3000/health
```

期望返回：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "ok",
    "timestamp": "2026-08-11T08:00:00.000Z"
  }
}
```

### 4. 测试 404 处理

```bash
curl http://localhost:3000/api/notfound
```

期望返回：
```json
{
  "code": 404,
  "message": "接口不存在",
  "error": null
}
```

---

## 📊 项目结构检查

此时你的项目应该是这样的：

```
node_lean/
├── src/
│   ├── config/
│   │   └── database.js          ✅
│   ├── middlewares/
│   │   └── errorHandler.js      ✅
│   ├── utils/
│   │   ├── logger.js            ✅
│   │   └── response.js          ✅
│   └── app.js                   ✅
├── logs/                        (自动生成)
│   ├── error.log
│   └── combined.log
├── uploads/                     ✅
├── docs/                        ✅
│   ├── 项目规划.md
│   ├── 开发进度清单.md
│   └── API接口文档.md
├── .env                         ✅
├── .gitignore                   ✅
├── package.json                 ✅
├── server.js                    ✅
└── README.md                    ✅
```

---

## 🎉 第一阶段完成！

恭喜！你已经完成了：

- [x] 项目初始化
- [x] 依赖安装
- [x] 基础目录结构
- [x] 数据库连接
- [x] 统一返回结构
- [x] 统一异常处理
- [x] 日志系统
- [x] 服务启动
- [x] 数据库创建（node_lean，utf8mb4字符集）
- [x] 健康检查接口测试通过（/health）
- [x] 404错误处理测试通过

**✅ 完成时间：2026-08-12**  
**✅ 服务运行在：http://localhost:3000**

**在进度清单中勾选完成的任务！**

---

## 📝 下一步

现在可以继续 **第二阶段：认证与授权**，包括：

1. 创建数据模型（User、Role、Permission）
2. 实现用户注册登录
3. JWT 认证中间件
4. RBAC 权限系统

参考文档：
- [开发进度清单.md](./开发进度清单.md) - 第二阶段任务列表
- [项目规划.md](./项目规划.md) - 详细技术方案
- [API接口文档.md](./API接口文档.md) - 接口设计

---

## ❓ 常见问题

### Q1: 数据库连接失败？
**A**: 检查 `.env` 中的数据库配置是否正确，确保 MySQL 服务已启动。

### Q2: 端口被占用？
**A**: 修改 `.env` 中的 `PORT`，或找到占用进程并关闭。

### Q3: 依赖安装失败？
**A**: 尝试切换 npm 源：
```bash
npm config set registry https://registry.npmmirror.com
```

### Q4: nodemon 不工作？
**A**: 确保安装了 nodemon：
```bash
npm install -g nodemon
# 或在项目中
npm install --save-dev nodemon
```

---

## 📚 推荐学习资源

- [Express 官方文档](https://expressjs.com/)
- [Sequelize 文档](https://sequelize.org/)
- [JWT 介绍](https://jwt.io/introduction)
- [RESTful API 设计指南](https://restfulapi.net/)

---

*继续加油！遇到问题随时查阅文档或搜索解决方案。*
